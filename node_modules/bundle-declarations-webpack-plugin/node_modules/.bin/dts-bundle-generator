#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../dts-bundle-generator/dist/bin/dts-bundle-generator.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../dts-bundle-generator/dist/bin/dts-bundle-generator.js" "$@"
  ret=$?
fi
exit $ret
